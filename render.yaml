services:
  - type: web
    name: sms-fraud-detector-backend
    env: python
    rootDir: be
    pythonVersion: 3.12.8
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn be.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true
